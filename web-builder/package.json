{
  "name": "cmdx-web-builder",
  "version": "0.1.0",
  "description": "Browser-based visual theme editor for cmdX — build theme.json files with live preview, no hand-written JSON required.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
